---
name: PsychPro landing hero — full-bleed splash artwork
description: Current landing hero (2026-07-23 PM): owner-supplied full-bleed blue-splash image as hero background (chrome brain removed from the asset per follow-up order); earlier ink-cloud and chrome-only compositions retired.
---

- CURRENT hero (2026-07-23, owner-supplied image + explicit spec, then amended): one full-bleed background image (`hero-ink-splash.png` — blue ink splashes; the silver chrome brain that was baked into the owner's original was REMOVED per follow-up order "remove the chrome brain only" via CV mask + diffusion fill on the actual asset) rendered edge-to-edge directly beneath the sticky nav. Owner's exact constraints: NO card/box/framed container, NO padding/margins/border-radius/borders/shadows around the image, NO max-width on the image or any parent, `object-fit: cover` (never contain), NO gray fallback background. Owner ALSO rejected pushing the text down with a spacer ("no dont push the text") — text keeps its natural top stacking (wordmark → tagline → headline → blurb → CTAs) overlaying the artwork with z-index:1. `.landing-hero` is full-width (max-width:none) with `min-height: calc(100svh - 58px)`; `.landing-hero-bg` is absolute inset:0 cover.
- Guardrail (check-design-drift.mjs) now pins: `.landing-hero-bg` <img> must exist AND its CSS must contain `object-fit: cover`.
- 2026-07-23 (later): owner supplied a NEW standalone transparent chrome-brain PNG and an EXACT spec (path public/assets/psychpro/psychpro-chrome-brain.png, class `psychpro-hero__brain`, absolute left:50%/top:47%, z-index:3, clamp(360px,34vw,700px), translate(-50%,-50%), no container/effects, preserve image exactly) plus exact <img> markup (root-relative src, alt "Chrome anatomical brain"). The spec'd absolute top:47% overlapped the blurb/CTAs; owner flagged it ("obviously this is an issue"), so the brain now sits IN FLOW between tagline and headline (display:block, margin auto, clamp width) — text flows around it, nothing overlaps. Lesson: when the owner supplies exact CSS that collides with text, implement verbatim first but expect a follow-up; the fix he accepts is in-flow placement, not moving text.
- Hero history, all same-week owner orders: splash brain → ink-cloud band + chrome cutout (07-22) → chrome brain alone (07-23 AM, "remove the blue design") → this full-bleed image (07-23 PM, owner supplied the art). Retired assets deleted: ink-cloud-band-cutout.webp, chrome-brain-cutout.webp. Owner iterates fast — expect further hero changes; always update the guardrail lock in the SAME commit.
- Entrance animation: the bg image is NOT in the opacity-0 entrance lists (shows instantly); text elements keep the staggered --delay reveal. `.landing-hero-art`/`.landing-hero-chrome`/`.landing-hero-ink` classes no longer exist.
- 2026-07-23 (readability pass, owner-approved): soft WHITE radial legibility glow behind the headline/blurb/CTA zone via `.landing-hero::before` at z-index 1 (above the z-0 bg image, below text — a negative z-index hides it under the bg; that was the old scrim's silent-failure mode). Brain resized smaller + blurb trimmed to ~2 lines (owner approved trimming his copy) so headline→CTAs fit above the fold; brain joins the staggered entrance via opacity-only transition (transform is owned by a subtle infinite float animation gated on .is-mounted with prefers-reduced-motion opt-out — never put translateY entrance on the same element or they fight).
- Owner previously (07-23 AM) angrily rejected crop/fade/mask attempts on hero art; the current object-fit:cover crop was explicitly ORDERED by the owner for this image — that ban applied to the old cloud cutout, not to cover on this background.
- Brain Lab promo keeps its grayscale lateral brain image (owner restored it 2026-07-22 — "remove brain images" applied to hero designs only).
- **Why:** owner supplied the exact artwork and a precise no-container spec; deviations (boxes, contain, gray fills) are explicit violations.

**2026-07-25 update:** Owner-supplied hero asset (`hero-ink-splash.png`, 4180×2353) now has the chrome brain BAKED IN — the standalone chrome-brain <img> was removed (a `.psychpro-hero__brain-spacer` keeps the text stack below the baked brain). Band is flipped `scale(-1,-1)` (pure vertical mirror read as a visible butterfly fold). Band overlays (::before/::after) must be `z-index:1` and sections `z-index:2` — at equal z-index, pseudo-elements lose to child elements (img covered the wash) but ::after beats sections in tree order (washed out card text). Mobile: fixed-height top crop (128vw) + 100vw spacer + white dissolve at the crop edge; the mobile spacer override must come AFTER the base rule in source order.

**2026-07-25 (later) update:** Hero artwork replaced AGAIN with an owner asset that has NO baked-in brain (white center, teal edge ink). The chrome brain is back as a standalone in-flow <img> (public/assets/psychpro/psychpro-chrome-brain.png via BASE_URL) at top-center with a "breathing" animation — transform scale only (4.8s ease-in-out), STATIC drop-shadow (animating filter on a huge PNG is repaint-heavy), prefers-reduced-motion honored. PSYCHPRO wordmark + tagline now sit UNDER the brain. The brain-spacer and the mobile 128vw top-crop + dissolve rules are GONE (plain cover works — image center is white). Band mirror + white washes unchanged and still needed for card legibility.

**2026-07-25 (fixed backdrop):** Owner then ordered the landing artwork VIEWPORT-FIXED with content scrolling over it. `.landing-hero-bg` is now position:fixed, z-index:-1; `.landing-root` needs `isolation: isolate` or its own white background paints OVER the negative-z fixed layer (root is positioned → its bg wins otherwise). The mirrored continuation band <img> + seam/wash overlays are DELETED (`.landing-artwork-band` is a plain wrapper now).

**2026-07-31 (asset recompression):** Both hero assets now ship as bundler-imported WebP (splash 2560w, chrome brain 1200w with alpha), replacing the ~10 MB source PNGs (recoverable from git history). The chrome brain no longer lives in `public/` — an unhashed public PNG was also the source of a persistent 403 console error. Rule: hero art is recompressed, never redesigned — visual output must stay identical to the owner's asset.
